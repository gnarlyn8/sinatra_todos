<ul id="lists">
  <% @lists.each_with_index do |list, index| %>
    <a href="/lists/<%= index %>">
      <li>
        <h2><%= list[:name] %></h2>
        <p><%= list[:todos].size %></p>
      </li>
    </a>
  <% end %>
</ul>

<a href="/lists/new">Add New Todo List</a>